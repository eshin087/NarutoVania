# Folder cleanup — 2026-09-08

## Scope and measurements

Inventoried all project files, including ignored files and Git storage. Before: 1251.26 MiB. After removing generated outputs: approximately 938.89 MiB. Reclaimed 312.37 MiB (25.0%). Measurements are logical file sizes, not filesystem allocation; documentation/Git updates can change the final size slightly.

Removed disposable QA screenshots, recordings, rendered audio audition output and logs; static build output; Vite dependency cache; framework caches; and TypeScript incremental output. A fresh build recreated its outputs for verification, then those outputs were removed again. Each deletion was checked against the resolved project root and an explicit list of generated paths.

Retained installed dependencies so local development does not require reinstalling, Git history, environment/hosting configuration, all runtime assets and source code, art prompts/manifests, licensed audio sources, source-preparation utilities, QA scripts and compact JSON evidence. Older art versions are cumulatively loaded by the current renderer; version age alone is not evidence that they are unused. No speculative source or asset deletions were made.

## Verification

- Type checking and lint passed.
- All 218 tests in 22 files passed.
- Production build passed from the retained inputs.
- Automated Chromium/Edge static-production smoke initialized all four fights through Scene Select, ran selected story scenes and an ultimate, and reached victory through the natural snowy-ending sequence.
- Browser result: no page errors and no missing asset responses. Development playtest controls were absent from production; current generated art was requested and removed manga/portrait assets were not requested.
- This was a focused production smoke, not a new full combat playthrough, physical-controller test or subjective audio review. Gameplay and runtime assets were unchanged.
- All 36 local links in the chapter workflow and README resolved before publication.

## Repository changes

Added the root AGENTS.md and chapter-production guides/templates. README now displays the exact reusable prompt near the top:

> Make a new chapter about [manga moment/fight]. Follow the chapter workflow.

Generated folders remain ignored. Run npm run dev to work locally or npm run build to recreate dist/client. A dependency reinstall is unnecessary. Deleting node_modules would save additional space but was intentionally avoided because those packages are needed to run and build the project.
